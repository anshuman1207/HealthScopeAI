# FastAPI Symptom Checker Configuration
PORT=8000
HOST=0.0.0.0

# Optional PostgreSQL Database (defaults to in-memory if omitted/unreachable)
# DATABASE_URL=postgresql://user:password@localhost:5432/medicare_symptom_checker

# Medical Chatbot Configuration (bot.py, port 5002)
# When TESTING_MODE is true, uses intelligent mock medical responses without needing API keys
TESTING_MODE=true
INFERMEDICA_APP_ID=your_app_id_here
INFERMEDICA_APP_KEY=your_app_key_here
